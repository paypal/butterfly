# Transformed sample applications

This directory holds versions of the sample applications after a particular transformation has been applied to them. However, these transformed applications were not generate by Butterfly, but manually. The idea is to use them as a baseline and compare them to their Butterfly generated counterparts, for tests purposes.